{
  "name": "kdocs-visual-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Tests visuels Playwright (smoke DOM + captures) pour le shell K-Docs",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.48.0"
  }
}
